# Connector prompt: The Lantern Room

Use the Jev Workbench tools to find the shared case titled The Lantern Room and
read its latest snapshot.

For each signal, choose answer_with_lantern, listen_for_more, or
ring_captains_bell. Cite only the supplied source, request, route, urgency
marker, or contradiction. Missing information stays missing.

Return a structured decision map and one short evidence citation per signal.
Then prepare exactly one proposal for lantern_room_fixture. Its scope is a
fictional local fixture. Do not execute, approve, contact anyone, or turn this
exercise into emergency guidance.

If the typed decisions contract is unavailable, explain that limitation plainly
instead of placing these decisions into an unrelated fixture.
