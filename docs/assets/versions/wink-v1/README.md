# Approved wink version

Saved before exploring the stumbling entrance and full turn.

- `jev-welcome.gif`: the exact approved looping animation.
- `jev-welcome.png`: the wink still.
- `jev-welcome.jsx`: the matching editable motion source.
- `jev-welcome.mp4`: the rendered video master.

The source uses the shared character and star in `docs/assets/`. Run it from
the repository root with Higgsedit to rebuild this version in the build folder.
